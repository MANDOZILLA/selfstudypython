import type { DiagnosticItem } from "../lib/diagnostic-types";

/** Stable authored IDs. Changes to a rubric or executable contract require a version bump. */
export const diagnosticSkills = [
  { id: "variables", title: "Variables and types", prerequisites: [], core: true, curriculumSkills: ["python-functions"] },
  { id: "strings", title: "Strings", prerequisites: ["variables"], core: true, curriculumSkills: ["data-structures"] },
  { id: "conditionals", title: "Conditionals", prerequisites: ["variables"], core: true, curriculumSkills: ["python-functions"] },
  { id: "loops", title: "Loops and nested loops", prerequisites: ["conditionals"], core: true, curriculumSkills: ["data-structures"] },
  { id: "functions", title: "Functions and scope", prerequisites: ["variables", "conditionals"], core: true, curriculumSkills: ["python-functions"] },
  { id: "collections", title: "Lists, dicts, sets and tuples", prerequisites: ["variables", "loops"], core: true, curriculumSkills: ["data-structures"] },
  { id: "exceptions", title: "Exceptions and debugging", prerequisites: ["functions"], core: true, curriculumSkills: ["csv-cleaning", "json-validation"] },
  { id: "reasoning", title: "Code reasoning", prerequisites: ["functions", "collections"], core: true, curriculumSkills: ["python-functions"] },
  { id: "comprehensions", title: "Comprehensions", prerequisites: ["loops", "collections"], core: false, curriculumSkills: ["data-structures"] },
  { id: "modules", title: "Modules and imports", prerequisites: ["functions"], core: false, curriculumSkills: ["csv-cleaning"] },
  { id: "files", title: "File input and output", prerequisites: ["exceptions", "strings"], core: false, curriculumSkills: ["csv-cleaning"] },
  { id: "csv-json", title: "CSV and JSON", prerequisites: ["files", "collections"], core: false, curriculumSkills: ["csv-cleaning", "json-validation"] },
  { id: "classes", title: "Basic classes", prerequisites: ["functions"], core: false, curriculumSkills: [] },
  { id: "http", title: "APIs and HTTP", prerequisites: ["exceptions", "csv-json"], core: false, curriculumSkills: ["json-validation"] },
  { id: "data", title: "Basic data manipulation", prerequisites: ["collections", "loops"], core: false, curriculumSkills: ["data-structures"] },
];
const short = (id: string, skillId: string, difficulty: 1|2|3, title: string, prompt: string, accepted: string[], hint: string): DiagnosticItem => ({ id, skillId, difficulty, kind: "short", title, prompt, rubric: { accepted }, hint, expectedBehavior: "Give the requested value or short explanation. Whitespace and capitalization in prose are ignored.", context: id });
const code = (skillId: string, title: string, prompt: string, expectedBehavior: string, hint: string, difficulty: 2|3 = 2): DiagnosticItem => ({ id: `${skillId}-code`, skillId, difficulty, title, prompt, expectedBehavior, hint, kind: "code", context: `${skillId}-implementation`, graderId: `diag-${skillId}-v1`, starter: "def solve(value):\n    # Write your code here.\n    pass\n" });

export const diagnosticItems: DiagnosticItem[] = [
  short("variables-read", "variables", 2, "Read the value and type", "x = '2'\nx += '3'\nWhat is x, and what is its Python type? Give value, type.", ["23 str", "23 string"], "Consider what + does to two strings."),
  short("variables-easy", "variables", 1, "Track a reassignment", "count = 4\ncopy = count\ncount = 9\nWhat value does copy hold now?", ["4"], "Assigning an integer binds a name to its current value."),
  code("variables", "Convert a whole number", "Implement solve(value) for a string containing a signed whole number, possibly surrounded by spaces. Return that integer multiplied by 2. Inputs are always valid integer strings.", "solve(' 7 ') → 14; solve('-3') → -6. Return an int, including for zero.", "Convert the text to an integer before doing arithmetic.", 3),
  code("strings", "Normalize a label", "Implement solve(value) for a string. Remove leading/trailing whitespace, lowercase it, and replace each space inside the trimmed value with a hyphen. Keep repeated internal spaces as repeated hyphens.", "solve('  Red Apple ') → 'red-apple'; solve('a  b') → 'a--b'; solve('   ') → ''.", "String methods return new strings; connect their returned values."),
  short("strings-easy", "strings", 1, "Read a slice", "word = 'python'\nWhat string does word[1:4] produce?", ["yth"], "The start index is included; the stop index is excluded."),
  short("strings-hard", "strings", 3, "Debug an unchanged string", "name = ' Ada '\nname.strip()\nprint(name == 'Ada')\nWrite the printed boolean, then one corrected assignment that trims name.", ["false name = name.strip()", "false name=name.strip()"], "Strings are immutable, and the result of strip is currently discarded."),
  code("conditionals", "Choose a delivery fee", "Implement solve(value), where value is a nonnegative integer order total. Return 0 for totals of at least 50, 3 for totals from 20 through 49, otherwise 5.", "solve(19) → 5; solve(20) → 3; solve(50) → 0. Return an integer fee.", "Check the highest threshold before the lower threshold."),
  short("conditionals-easy", "conditionals", 1, "Evaluate both conditions", "age = 17\nhas_pass = True\nWhat boolean does age >= 18 and has_pass evaluate to?", ["false"], "Both sides of and must be true."),
  short("conditionals-hard", "conditionals", 3, "Follow short circuit evaluation", "items = []\nprint(bool(items) and items[0] == 'ready')\nDoes this print a boolean or raise IndexError? Give the exact printed value, or the exception.", ["false", "prints false"], "The second expression of and is evaluated only when the first is truthy."),
  code("loops", "Add a grid", "Implement solve(value), where value is a list of rows and each row is a list of integers. Return the sum of every number, including negatives. Rows may be empty.", "solve([[1, 2], [], [-4, 8]]) → 7; solve([]) → 0. Work across every row.", "Keep a running total while visiting rows and their numbers."),
  short("loops-easy", "loops", 1, "Trace a small range", "total = 0\nfor n in range(1, 4):\n    total += n\nWhat is total?", ["6"], "range stops before its second argument."),
  short("loops-hard", "loops", 3, "Trace an inner break", "count = 0\nfor row in [[1, 0, 2], [3, 4]]:\n    for n in row:\n        if n == 0:\n            break\n        count += 1\nWhat is count?", ["3"], "A break exits only its innermost loop."),
  code("functions", "Return a running balance", "Implement solve(value), where value is a tuple (opening_balance, changes). changes is a list of signed integers. Return opening_balance plus all changes. Do not modify changes or rely on state left over from a previous call.", "solve((10, [2, -4])) → 8; solve((3, [])) → 3. Repeated calls must work independently.", "Keep the balance local to the function and return it."),
  short("functions-easy", "functions", 1, "Distinguish print and return", "def double(n):\n    print(n * 2)\nresult = double(3)\nWhat value is stored in result? Ignore console output.", ["none"], "A function without return finishes with Python's no-value object."),
  short("functions-hard", "functions", 3, "Read local scope", "x = 5\ndef bump(x):\n    x += 1\n    return x\ny = bump(x)\nGive x, y after the call.", ["5 6"], "The parameter name belongs to the function's local scope."),
  code("collections", "Count distinct tags", "Implement solve(value), where value is a list of (name, tag) tuples. Return a dict mapping each name to the number of distinct tags seen for that name. Names and tags are strings and case-sensitive.", "solve([('Ada', 'py'), ('Ada', 'py'), ('Ada', 'sql'), ('Bo', 'py')]) → {'Ada': 2, 'Bo': 1}; solve([]) → {}.", "A dictionary can group by name; a set removes repeated tags within each group."),
  short("collections-easy", "collections", 1, "Look up a missing key", "counts = {'red': 2}\nWhat does counts.get('blue', 0) return?", ["0"], "The second argument to get is the fallback for a missing key."),
  short("collections-hard", "collections", 3, "A tuple containing a list", "pair = ([1], 'x')\npair[0].append(2)\nDoes this raise TypeError? If not, give the contents of pair[0].", ["[1, 2]", "no [1, 2]"], "Tuple slots cannot be reassigned, but an object stored in a slot may be mutable."),
  short("exceptions-read", "exceptions", 2, "Locate the conversion failure", "for text in ['4', 'oops', '7']:\n    print(int(text))\nName the exception raised, and the first input that triggers it.", ["ValueError oops"], "The second string cannot be interpreted as a whole number."),
  { id: "exceptions-easy", skillId: "exceptions", difficulty: 1, kind: "short", title: "Repair one bad row", prompt: "A loop converts each row with int(text). One invalid text currently stops the entire import. Describe where to catch the specific exception and how to keep later rows processing.", expectedBehavior: "Name the exception, the conversion to protect, and what happens to the invalid row and later rows.", hint: "Limit the exception handler to the conversion for one row.", context: "row-repair", rubric: { elements: ["(catch|except|handle).{0,35}valueerror", "(int|integer|conversion|convert)", "(skip|continue|ignore).{0,30}(row|record|loop)|continue.{0,20}(processing|loop)", "(keep|later|other|next|remaining).{0,35}(row|processing|record)"], reject: ["do not catch|don't catch|stop processing all|catch all exceptions|except baseexception"] } },
  code("exceptions", "Keep valid integer readings", "Implement solve(value) for a list of strings. Convert valid signed integer strings into integers, keep their order, and skip only strings that raise ValueError. Whitespace is allowed; a decimal string such as '2.5' is invalid.", "solve([' 3 ', 'bad', '-1', '2.5', '7']) → [3, -1, 7]; solve([]) → [].", "Catch ValueError inside the loop so one bad reading does not stop later ones.", 3),
  short("reasoning-read", "reasoning", 2, "Track shared references", "first = [1]\nsecond = first\nsecond.append(2)\nWhat are the contents of first?", ["[1, 2]"], "Both names refer to the same list."),
  short("reasoning-easy", "reasoning", 1, "Trace a return", "def select(n):\n    if n > 0:\n        return 'yes'\n    return 'no'\nWhat does select(0) return?", ["no"], "Zero is not greater than zero."),
  code("reasoning", "Repair a default argument bug", "The buggy function remembers earlier calls: def solve(value, saved=[]): saved.append(value); return saved. Replace it with solve(value) that returns a fresh one-item list containing value on every call. Mutating an earlier result must not affect later results.", "Calling solve('a') then solve('b') returns ['a'] then ['b']. solve(None) → [None].", "Construct the result inside each call, with no persistent default list.", 3),
  short("comprehensions-read", "comprehensions", 2, "Trace a filtered comprehension", "values = [1, 2, 3, 4]\nGive the value of [n * n for n in values if n % 2 == 0].", ["[4, 16]"], "Filter each input before calculating its output."),
  short("comprehensions-easy", "comprehensions", 1, "Read the mapping expression", "What list does [n + 1 for n in [0, 2]] create?", ["[1, 3]"], "Apply the expression once per input item."),
  code("comprehensions", "Transform positive readings", "Implement solve(value), a list comprehension that returns the square of every strictly positive integer in the supplied list, preserving order. Zero and negatives are omitted.", "solve([-2, 0, 3, 1]) → [9, 1]; solve([]) → []. Use a list comprehension in the executed function.", "Place the filter after the for clause in the comprehension.", 3),
  short("modules-read", "modules", 2, "Use an imported alias", "from math import sqrt as root\nWhich single expression computes the square root of 81 using this import?", ["root(81)"], "Only the alias root was added to this namespace."),
  short("modules-easy", "modules", 1, "Read a module call", "import math\nWhat integer does math.floor(3.8) return?", ["3"], "floor moves to the greatest integer no larger than the value."),
  code("modules", "Round each measurement up", "Implement solve(value), where value is a list of finite numbers. Use the math module to return a list of integers rounded upward with ceil. Inputs may be negative or already whole.", "solve([2.1, -2.1, 4.0]) → [3, -2, 4]; solve([]) → []. Use math.ceil or an imported alias.", "Import ceil from math, or access it through the module.", 3),
  short("files-read", "files", 2, "Protect an existing file", "You want to add a line at the end of an existing UTF-8 log without erasing it. What one-letter mode should open('log.txt', mode, encoding='utf-8') use?", ["a", "append a"], "Write mode truncates the old contents; choose append mode."),
  short("files-easy", "files", 1, "Read a file as text", "with open('notes.txt', 'r', encoding='utf-8') as handle:\n    text = handle.read()\nIs text a str, bytes, or list? Write the type name.", ["str", "string"], "The file was opened in text mode."),
  code("files", "Read nonempty lines", "Implement solve(value), where value is the path to an existing UTF-8 text file. Open and read that file. Return its lines with surrounding whitespace removed, omitting lines that become empty. Close the file using a with statement.", "A file containing ' Ada \\n\\nBo\\n' should produce ['Ada', 'Bo']. An empty file produces []. The grader supplies real temporary files.", "Use with open(..., encoding='utf-8') and process each line.", 3),
  short("csv-json-read", "csv-json", 2, "Read a quoted CSV field", "The CSV row is:\n7,\"Ada, Jr.\",active\nHow many fields does a CSV parser read? What is the second field? Give count, field value.", ["3 Ada, Jr."], "A comma inside a quoted field is data, not a delimiter."),
  short("csv-json-easy", "csv-json", 1, "Read a JSON object", "import json\ndata = json.loads('{\"ready\": true}')\nWhat Python boolean is data['ready']?", ["true"], "JSON booleans become Python booleans."),
  code("csv-json", "Extract names from JSON", "Implement solve(value), where value is a valid JSON string containing a list of objects. Use json.loads and return the name field from every object, in order. All objects contain a string name; names may contain commas or non-ASCII characters.", "solve('[{\"name\": \"Ada, Jr.\"}, {\"name\": \"Bo\"}]') → ['Ada, Jr.', 'Bo']; solve('[]') → [].", "Parse the JSON before iterating over the resulting list.", 3),
  short("classes-read", "classes", 2, "Follow instance state", "class Counter:\n    def __init__(self): self.n = 0\n    def tick(self): self.n += 1\na, b = Counter(), Counter()\na.tick()\nGive a.n, b.n.", ["1 0"], "Each call to Counter creates a separate instance."),
  short("classes-easy", "classes", 1, "Name the initializer", "Which special Python method initializes a newly created class instance? Give its name, including underscores.", ["__init__"], "It runs when the usual class constructor initializes an instance."),
  code("classes", "Build a small counter", "Define class Counter with __init__(start=0), an integer attribute value, and increment() that adds 1 and returns the new value. Also implement solve(value): construct Counter(value), call increment twice, return its value. Each instance must keep its own state.", "solve(4) → 6; Counter().increment() → 1. Two counters must remain independent.", "Store the count on self, not on the class.", 3),
  {id:"http-read",skillId:"http",difficulty:2,kind:"short",title:"Read an HTTP failure",prompt:"An API responds with HTTP 429 and a Retry-After header. Give the status meaning and describe what your client should do before another request.",expectedBehavior:"Explain what 429 means and how the header affects when to try again.",hint:"The service limits request frequency; the header specifies a delay.",context:"http-rate-limit",rubric:{accepted:["too many requests wait retry-after","rate limited wait retry-after","rate limit wait retry-after"],elements:["rate limit|too many requests","wait|delay|back.?off","retry.?after"],reject:["immediate|ignore|do not wait|don't wait|without waiting"]}},
  short("http-easy", "http", 1, "Choose a retrieval method", "Which HTTP method normally retrieves a resource without requesting a change to it? Write just the method.", ["get"], "It is the method commonly used to fetch a page or resource."),
  code("http", "Handle a simulated response", "Implement solve(value) for a dict with integer status and payload. For any 2xx status return payload; for 404 return None; for every other status raise ValueError. The inputs simulate HTTP; do not make network requests.", "solve({'status': 201, 'payload': {'id': 7}}) → {'id': 7}; 404 → None; 429 and 500 raise ValueError.", "Check the complete success range before handling the specific not-found case.", 3),
  short("data-read", "data", 2, "Reason about an average", "A sensor records [10, None, 20]. Missing values must be excluded. What is the mean of the observed readings?", ["15", "15.0"], "Divide by the count of observed readings, not all list entries."),
  short("data-easy", "data", 1, "Read a sorted result", "What list does sorted([3, 1, 2]) return?", ["[1, 2, 3]"], "The default ordering for numbers is ascending."),
  code("data", "Group sales totals", "Implement solve(value), a list of records with string category and integer amount. Return a dict of total amount per category. Negative amounts are refunds and must be included. Do not change the input records.", "solve([{'category':'books','amount':5},{'category':'books','amount':-2}]) → {'books':3}; solve([]) → {}.", "Accumulate amounts in a dictionary keyed by category.", 3),
];
