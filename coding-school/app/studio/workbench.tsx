import Editor, { loader } from "@monaco-editor/react";
import { useEffect, useState } from "react";
import type { Studio } from "./use-studio";
import { StageRail } from "./stage-rail";
import { nextWorkbenchPanel, workbenchPanels, type WorkbenchPanel } from "../../lib/studio";

loader.config({ paths: { vs: "/monaco/vs" } });

function TaskInstructions({ studio }: { studio: Studio }) {
  const { workbench, draft } = studio;
  const task = workbench?.task;
  if (!task) return null;
  const independentForTask = studio.learningMode && !studio.assistanceUsed;
  return <div className="task-content">
    <span className="eyebrow">YOUR TASK · {task.purpose.replaceAll("-", " ")}</span><h2>{task.title}</h2>
    <div className="task-outcome"><span className="eyebrow">OUTCOME</span><p>{task.kind === "code" ? task.purpose === "project" ? "A working Python artifact that meets every requirement below." : "A small working transformation you can reuse in your project." : task.kind === "explanation" ? "A saved explanation of your decisions and remaining questions." : "Understand the pattern before using it in guided practice."}</p></div>
    <section className="teaching-block pattern-block"><h3>{task.kind === "instruction" ? "Inspect the pattern" : "What to do"}</h3><p className="task-explanation">{task.explanation}</p></section>
    <section className="teaching-block requirement-block"><h3>Requirements</h3><ol className="requirements">{task.requirements.map(item => <li key={item}>{item}</li>)}</ol></section>
    <section className="teaching-block example-block"><h3>{task.kind === "instruction" ? "Worked examples" : "Example input / expected output"}</h3>{task.examples.map((example, index) => <figure key={index}><figcaption>{task.kind === "instruction" ? "Worked example" : "Input and expected output"} {index + 1}</figcaption><pre className="example">{example}</pre></figure>)}</section>
    {task.kind === "code" && independentForTask && <p className="independent-note">Independent work · Hints and solutions stay hidden. Turn Learning Mode off to use and record help.</p>}
    {task.kind === "code" && draft && !independentForTask && <section className="hint-section"><div className="section-heading"><h3>Need a nudge?</h3><span className="muted">{draft.assistance.hintsUsed}/{task.hints.length} hints</span></div><p className="muted">Reveal one hint at a time. Hints are saved with your attempt.</p>{task.hints.slice(0, draft.assistance.hintsUsed).map((hint, i) => <p className="hint" key={hint}><strong>Hint {i + 1}</strong>{hint}</p>)}{draft.assistance.hintsUsed < task.hints.length && <button className="secondary" onClick={() => studio.updateDraft({ assistance: { ...draft.assistance, hintsUsed: draft.assistance.hintsUsed + 1 } })}>Reveal hint {draft.assistance.hintsUsed + 1}</button>}<details className="assistance-disclosure"><summary>Record other help</summary><p className="muted">If you used help outside this app, include it in this attempt.</p><label><input type="checkbox" checked={draft.assistance.aiAssisted} disabled={draft.assistance.aiAssisted} onChange={() => studio.updateDraft({ assistance: { ...draft.assistance, aiAssisted: true } })} /> I used external AI assistance</label><label><input type="checkbox" checked={draft.assistance.solutionViewed} disabled={draft.assistance.solutionViewed} onChange={() => studio.updateDraft({ assistance: { ...draft.assistance, solutionViewed: true } })} /> I viewed a solution</label></details></section>}
  </div>;
}

function Checks({ studio }: { studio: Studio }) {
  const { result, status, busy, attemptSaved, workbench } = studio;
  const restored = !result && workbench?.taskComplete ? workbench.latestAttempt : undefined;
  return <section className="checks-panel" aria-label="Checks and output" tabIndex={-1} id="checks-panel">
    <div className="checks-heading"><h2>Checks</h2><span className={`run-status ${status === "Passed" ? "success" : ["Needs changes", "Timed out", "Couldn't run"].includes(status) ? "failure" : ""}`} role="status" aria-live="polite">{restored ? restored.passed ? "Passed · saved attempt" : "Review attempted" : status}</span></div>
    {busy ? <p className="checks-message">{status === "Loading Python" ? "Starting Python in your browser. Your code stays here." : "Testing your returned values and required Python concepts."}</p> : result ? <>
      <p className="checks-message">{result.passed ? attemptSaved ? workbench?.task?.purpose === "project" ? "Required checks passed. Your project attempt is saved." : "Required checks passed. Your practice attempt is saved." : "Checks passed, but this attempt has not been saved. Retry saving or run again." : attemptSaved ? "Attempt saved. Use the checks below to decide what to change." : "This attempt has not been saved. Your code is preserved."}</p>
      <ul className="check-results">{result.tests.map(check => <li key={check.id} className={check.passed ? "check-pass" : "check-fail"}><span aria-hidden="true">{check.passed ? "✓" : "×"}</span><div><strong>{check.name}</strong>{check.detail && <p>{check.detail}</p>}{!check.passed && <p className="next-action">Next: {check.id === "execution" ? "Check the message below, then try Run checks again." : "Update the code for this requirement, then run the checks again."}</p>}</div></li>)}</ul>
    </> : restored ? <p className="checks-message">{restored.passed ? "This draft matches your saved passed attempt. Continue when you’re ready." : "Your review attempt is saved. You can continue or try again."}</p> : <p className="checks-message">Run your code against the requirements. Printing an answer alone does not pass the checks.</p>}
    <details className="console-output"><summary>Console output <span>{result?.stderr ? "· error output" : ""}</span></summary><pre>{result ? `${result.stdout || "No standard output."}${result.stderr ? `\n\n${result.stderr}` : ""}` : "No output yet. Run checks to execute your code."}</pre></details>
  </section>;
}

export function Workbench({ studio }: { studio: Studio }) {
  const [panel, setPanel] = useState<WorkbenchPanel>("instructions");
  const [plainEditor, setPlainEditor] = useState(false);
  const [mobile, setMobile] = useState(false);
  useEffect(() => {
    const query = window.matchMedia("(max-width: 1023px)");
    const sync = () => setMobile(query.matches);
    sync(); query.addEventListener("change", sync);
    return () => query.removeEventListener("change", sync);
  }, []);
  const { workbench, draft } = studio;
  if (!workbench) return null;
  const { mission, run, stage, task } = workbench;
  const nextStage = mission.stages[run.stageIndex + 1];
  const continueLabel = workbench.stageComplete ? run.mode === "review" ? "Finish review" : nextStage ? `Continue to ${nextStage.title}` : "Finish mission" : "Continue to next task";
  return <main className="workbench" id="main-content">
    <header className="workbench-heading"><div className="workbench-topline"><button className="text-button" onClick={() => studio.navigate("today")}>← Today</button><label className="learning-toggle"><input type="checkbox" checked={studio.learningMode} onChange={studio.toggleLearningMode} /><span>Learning Mode</span></label></div><div className="workbench-title"><h1 id="page-title" tabIndex={-1}>{run.mode === "review" ? "Scheduled skill review" : mission.title}</h1><span>{run.mode === "review" ? stage.estimatedMinutes : mission.estimatedMinutes} min {run.mode === "review" ? "review" : "mission"}</span></div><p className="mode-description">{studio.learningMode && studio.assistanceUsed ? "This task stays recorded as assisted. Independent Mode applies when you start the next task; prior help cannot be erased." : studio.learningMode ? "Independent work: hints and solutions are hidden; no assistance is recorded." : studio.assistanceUsed ? "Help is recorded for this task; past assistance stays in your record." : "Practice with authored hints, revealed one at a time. Any help you use is recorded."}</p><StageRail mission={mission} run={run} /></header>
    {!task ? <section className="stage-document document"><span className="eyebrow">REVIEW · {stage.estimatedMinutes} MIN PLANNED</span><h2>No retrieval is due for this mission.</h2><p>Your review schedule begins after you learn and practise. Use this moment to read the mission outcome, then begin the lesson.</p><div className="review-outcome"><span className="eyebrow">BY THE END</span><h3>{mission.stages[2].tasks[0]?.title}</h3><p>{mission.summary}</p></div><p className="muted">The stage times are a suggested 45-minute guide, not a countdown.</p><button className="primary" onClick={studio.continueStage}>Continue to Learn →</button></section> : task.kind !== "code" ? <section className="reading-layout"><article className="document reading-document"><TaskInstructions studio={studio} />{task.kind === "explanation" && draft && <div className="reflection-input"><label htmlFor="reflection">Your explanation</label><textarea id="reflection" value={draft.response} onChange={event => studio.updateDraft({ response: event.target.value })} placeholder="Describe one decision, a concrete example, and what you’re still unsure about." rows={7} /><span className="muted">At least 20 characters. Your reflection is saved, not automatically graded.</span></div>}<footer className="reading-action"><p>{task.kind === "instruction" ? "Reading records exposure. Guided practice comes next." : "Save this reflection to complete your mission."}</p><button className="primary" disabled={task.kind === "explanation" && (draft?.response.trim().length ?? 0) < 20} onClick={studio.submitText}>{task.kind === "instruction" ? "I’ve read the examples · Start practice" : "Save reflection & finish"} →</button></footer></article><aside className="reading-aside"><span className="eyebrow">{stage.title.toUpperCase()}</span><h2>{task.kind === "instruction" ? "Understand it before you build it." : "Make your thinking visible."}</h2><p>{task.kind === "instruction" ? "Trace the examples in order. Notice what each function accepts, what it rejects, and what it returns." : "A passed check shows behavior. Your explanation captures why the behavior is correct."}</p><span className="reading-file">main.py<span>Next: {task.kind === "instruction" ? "guided practice" : "saved evidence"}</span></span></aside></section> : <>
      <div className="workbench-tabs" role="tablist" aria-label="Workbench panels">{workbenchPanels.map(item => <button key={item} role="tab" id={`tab-${item}`} aria-controls={`panel-${item}`} aria-selected={panel === item} tabIndex={panel === item ? 0 : -1} onClick={() => setPanel(item)} onKeyDown={event => { if (["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) { event.preventDefault(); const next = nextWorkbenchPanel(item, event.key); setPanel(next); document.getElementById(`tab-${next}`)?.focus(); } }}>{item === "instructions" ? "Instructions" : item === "code" ? "Code" : "Checks"}</button>)}</div>
      <div className={`coding-layout active-${panel}`}><section className="instruction-pane" id="panel-instructions" role={mobile ? "tabpanel" : "region"} aria-labelledby={mobile ? "tab-instructions" : undefined} aria-label={mobile ? undefined : "instructions"} hidden={mobile && panel !== "instructions"}><TaskInstructions studio={studio} /><button className="primary mobile-code-action" onClick={() => setPanel("code")}>Open code →</button></section><div className="coding-right"><section className="code-pane" id="panel-code" role={mobile ? "tabpanel" : "region"} aria-labelledby={mobile ? "tab-code" : undefined} aria-label={mobile ? undefined : "code"} hidden={mobile && panel !== "code"}><div className="editor-toolbar"><span>main.py <small>Python</small></span><button className="text-button" onClick={() => setPlainEditor(!plainEditor)}>{plainEditor ? "Use code editor" : "Use plain text"}</button></div><div className="editor-canvas">{plainEditor ? <textarea className="plain-editor" aria-label="Python code" value={draft?.sourceFiles["main.py"] ?? ""} spellCheck={false} onChange={event => studio.updateDraft({ sourceFiles: { "main.py": event.target.value } })} /> : <Editor path={`${run.id}/${task.id}/main.py`} language="python" theme="light" value={draft?.sourceFiles["main.py"] ?? ""} onChange={value => studio.updateDraft({ sourceFiles: { "main.py": value ?? "" } })} loading={<p className="editor-loading">Loading code editor… You can also choose “Use plain text”.</p>} options={{ minimap: { enabled: false }, fontFamily: "IBM Plex Mono, monospace", fontSize: 13, lineHeight: 22, padding: { top: 16 }, scrollBeyondLastLine: false, automaticLayout: true, wordWrap: "on", tabSize: 4, ariaLabel: "Python code editor. Press Escape then Tab to leave the editor.", accessibilitySupport: "on", fixedOverflowWidgets: true }} onMount={editor => { editor.onKeyDown(event => { if (event.code === "Escape") { event.preventDefault(); document.getElementById("run-checks")?.focus(); } }); }} />}</div><div className="editor-actions"><button id="run-checks" className="primary" disabled={studio.busy} onClick={() => { setPanel("checks"); studio.runChecks(); }}>{studio.busy ? studio.status : "Run checks"}<span aria-hidden="true"> ▷</span></button>{studio.busy ? <button className="secondary desktop-stop" onClick={studio.stopRun}>Stop run</button> : workbench.taskComplete && !studio.storageError && <button className="secondary continue-button" onClick={studio.continueStage}>{continueLabel} →</button>}<span className="save-indicator" role="status">{studio.storageError ? "Not saved" : "Draft saved locally"}</span></div><p className="editor-shortcut">Press Esc to leave the editor · Your code is saved as you type</p></section><div className="checks-wrap" id="panel-checks" role={mobile ? "tabpanel" : "region"} aria-labelledby={mobile ? "tab-checks" : undefined} aria-label={mobile ? undefined : "checks"} hidden={mobile && panel !== "checks"}><Checks studio={studio} /><div className="mobile-check-actions">{studio.busy && <button className="primary" onClick={studio.stopRun}>Stop run</button>}<button className="secondary" onClick={() => setPanel("code")}>← Edit code</button>{workbench.taskComplete && !studio.storageError && <button className="primary" onClick={studio.continueStage}>{continueLabel} →</button>}</div></div></div></div>
    </>}
  </main>;
}
